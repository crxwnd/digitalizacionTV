{
  "name": "digitalizaciontv-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Dev mode'",
    "build": "mkdir -p dist && cp index.html dist/"
  },
  "dependencies": {
    "react": "^18.2.0"
  }
}
